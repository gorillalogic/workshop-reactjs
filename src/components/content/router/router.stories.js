import React from 'react'
import { storiesOf } from '@storybook/react'
import { action } from '@storybook/addon-actions'

import ReactRouter from './reactRouter'

storiesOf('CONTENTS|Router', module).add('React router', () => <ReactRouter />)
